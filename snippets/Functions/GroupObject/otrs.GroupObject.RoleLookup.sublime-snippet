<snippet>
    <content><![CDATA[
${1:my \$RoleID = \$GroupObject->RoleLookup( Role => \$Role );
}${2:my \$Role = \$GroupObject->RoleLookup( RoleID => \$RoleID );
}${3:my \$RoleID = \$GroupObject->RoleLookup(
    Role => \$Role,
);
}${4:my \$Role = \$GroupObject->RoleLookup(
    RoleID => \$RoleID,
);
}
]]></content>
    <tabTrigger>otrs.GroupObject.RoleLookup</tabTrigger>
    <description>6, 5, 4</description>
</snippet>
