<snippet>
    <content><![CDATA[
${1:my %List = \$StateObject->StateList(
    UserID => \$UserID,
);
}${2:my %List = \$StateObject->StateList(
    UserID => \$UserID,
    Valid  => 1, # is default
);
}${3:my %List = \$StateObject->StateList(
    UserID => \$UserID,
    Valid  => 0,
);
}
]]></content>
    <tabTrigger>otrs.StateObject.StateList</tabTrigger>
    <description>4, 5</description>
</snippet>
