<snippet>
    <content><![CDATA[
${1:my %Watch = \$TicketObject->TicketWatchGet(
    TicketID => \$TicketID,
);
}${2:my %Watch = \$TicketObject->TicketWatchGet(
    TicketID => \$TicketID,
    Notify   => 1,
);
}${3:my @Watch = \$TicketObject->TicketWatchGet(
    TicketID => \$TicketID,
    Result   => 'ARRAY',
);
}
]]></content>
    <tabTrigger>otrs.TicketObject.TicketWatchGet</tabTrigger>
    <description>4, 5</description>
</snippet>
