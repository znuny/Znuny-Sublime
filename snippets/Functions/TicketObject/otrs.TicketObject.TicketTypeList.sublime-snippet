<snippet>
    <content><![CDATA[
${1:my %Types = \$TicketObject->TicketTypeList(
    UserID => \$UserID,
);
}${2:my %Types = \$TicketObject->TicketTypeList(
    QueueID => \$QueueID,
    UserID  => \$UserID,
);
}${3:my %Types = \$TicketObject->TicketTypeList(
    TicketID => \$TicketID,
    UserID   => \$UserID,
);
}
]]></content>
    <tabTrigger>otrs.TicketObject.TicketTypeList</tabTrigger>
    <description>4, 5</description>
</snippet>
