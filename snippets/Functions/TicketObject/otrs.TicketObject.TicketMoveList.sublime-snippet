<snippet>
    <content><![CDATA[
${1:my %Queues = \$TicketObject->TicketMoveList(
    Type   => 'create',
    UserID => \$UserID,
);
}${2:my %Queues = \$TicketObject->TicketMoveList(
    QueueID => \$QueueID,
    UserID  => \$UserID,
);
}${3:my %Queues = \$TicketObject->TicketMoveList(
    TicketID => \$TicketID,
    UserID   => \$UserID,
);
}${4:my %Queues = \$TicketObject->TicketMoveList(
    Type           => 'create',
    CustomerUserID => \$CustomerUserID,
);
}
]]></content>
    <tabTrigger>otrs.TicketObject.TicketMoveList</tabTrigger>
    <description>5, 4</description>
</snippet>
