<snippet>
    <content><![CDATA[
${1:my \$Access = \$TicketObject->TicketPermission(
    Type     => 'ro',
    TicketID => \$TicketID,
    UserID   => \$UserID,
);
}${2:my \$Access = \$TicketObject->TicketPermission(
    Type     => 'ro',
    TicketID => \$TicketID,
    LogNo    => 1,
    UserID   => \$UserID,
);
}
]]></content>
    <tabTrigger>znuny.ArticleObject.TicketPermission</tabTrigger>
    <description>6</description>
    <scope>source.perl</scope>
</snippet>
