<snippet>
    <content><![CDATA[
NEEDED:
for my \$Needed ( qw($1) ) {

    next NEEDED if defined \$Param{$2}->{ \$Needed };

    \$Self->{LogObject}->Log(
        Priority => 'error',
        Message  => "Parameter '\$Needed' is needed in $2!",
    );
    return;
}
]]></content>
    <tabTrigger>znuny.Needed.Subelement.Self</tabTrigger>
    <scope>source.perl</scope>
</snippet>
